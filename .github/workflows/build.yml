name: 'build'

on:
  push:
    paths:
      - 'Dockerfile'
    branches:    
      - main

jobs:
  build:
    name: 'Build'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: docker/build-push-action@v1
        with:
          registry: ghcr.io
          username: "gowerc"
          password: ${{ secrets.PAT }}
          repository: gowerc/pschecker
          tags: latest